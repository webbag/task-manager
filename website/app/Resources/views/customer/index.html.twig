{% extends 'base.html.twig' %}

{% block body %}
    <h1>Customer list</h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Cuid</th>
                <th>Cuname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for customer in customers %}
            <tr>
                <td><a href="{{ path('customer_show', { 'id': customer.cuId }) }}">{{ customer.cuId }}</a></td>
                <td>{{ customer.cuName }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('customer_show', { 'id': customer.cuId }) }}" class="btn btn-default" role="button">Podgląd</a>
                        </li>
                        <li>
                            <a href="{{ path('customer_edit', { 'id': customer.cuId }) }}" class="btn btn-primary" role="button">Edycja</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('customer_new') }}" class="btn btn-primary" role="button">Dodaj nowy</a>
        </li>
    </ul>
{% endblock %}
