{% extends 'base.html.twig' %}

{% block body %}
    <h1>Task</h1>

    <table>
        <tbody>
            <tr>
                <th>Taid</th>
                <td>{{ task.taId }}</td>
            </tr>
            <tr>
                <th>Taname</th>
                <td>{{ task.taName }}</td>
            </tr>
            <tr>
                <th>Tadescription</th>
                <td>{{ task.taDescription }}</td>
            </tr>
            <tr>
                <th>Tadatecreate</th>
                <td>{% if task.taDateCreate %}{{ task.taDateCreate|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Tapriority</th>
                <td>{{ task.taPriority }}</td>
            </tr>
            <tr>
                <th>Tastatus</th>
                <td>{{ task.taStatus }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('task_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('task_edit', { 'id': task.taId }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
